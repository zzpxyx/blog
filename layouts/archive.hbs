<!doctype html>
<html>

<head>
    {{> meta}}
    <title>{{pageTitle}}</title>
    {{> css}}
</head>

<body>
    {{> header}}
    <article>
        {{#each archive}}
        <h3>{{year}}</h3>
        <ul class="archive">
            {{#each data}}
            <li><time>{{formatDate publishDate}}</time><a href="/{{permalink}}/">{{title}}</a></li>
            {{/each}}
        </ul>
        {{/each}}
    </article>
</body>

</html>
