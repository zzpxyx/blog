<!doctype html>
<html>
    <head>
    </head>
    <body>
        <ul>
            {{#each collections.posts}}
                <li>
                    <h3><a href={{path}}>{{title}}</a></h3>
                    {{&excerpt}}
                </li>
            {{/each}}
        </ul>
    </body>
</html>
